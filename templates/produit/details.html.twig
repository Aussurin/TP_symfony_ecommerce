{% extends 'base.html.twig' %}

{% block body %}
    <h1> {{ produit.nom }}</h1>
    <img src="#" alt="image de {{ produit.nom }}">
    <h2>{{ produit.prix }}</h2>
    <p>{{ produit.description }}</p>
    <a href="#">Ajouter au panier</a>
{% endblock %}

{% block title %}
    Details {{ produit.nom }}
{% endblock %}

